version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      bash -c "python main.py"
    ports:
      - 80:8000
    image: wishbot-dev
    hostname: abra-api
    container_name: wishbot-dev
    volumes:
      - "./:/api"
    env_file:
      - .env
